import { createSlice } from "@reduxjs/toolkit";

const TossPayment = {
    orderid: "",
}


const initialState = Request

const tosspaymentsSlice = createSlice({
    name: 'tosspayments',
    initialState,
    reducers: {
        set: (state, action) => {

        }
    },
});

export default tosspaymentsSlice;